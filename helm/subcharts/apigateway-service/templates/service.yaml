apiVersion: v1
kind: Service
metadata:
  name: {{ include "apigateway-service.fullname" . }}
  labels:
    {{- include "apigateway-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      protocol: TCP
      targetPort: http
  selector:
    {{- include "apigateway-service.selectorLabels" . | nindent 4 }}
