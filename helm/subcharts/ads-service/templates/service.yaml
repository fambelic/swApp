apiVersion: v1
kind: Service
metadata:
  name: {{ include "ads-service.fullname" . }}
  labels:
    {{- include "ads-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      protocol: TCP
      targetPort: http
  selector:
    {{- include "ads-service.selectorLabels" . | nindent 4 }}
