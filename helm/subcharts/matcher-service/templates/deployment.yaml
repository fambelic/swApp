apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "matcher-service.fullname" . }}
  labels:
    {{- include "matcher-service.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "matcher-service.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "matcher-service.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ include "matcher-service.appName" . }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          env:
            - name: SPRING_DATA_MONGODB_HOST
              value: {{ include "matcher-service.peerFullname" (dict "root" . "component" .Values.dependencies.dbComponentName) | quote }}
            - name: SPRING_RABBITMQ_HOST
              value: {{ include "matcher-service.peerFullname" (dict "root" . "component" .Values.dependencies.rabbitComponentName) | quote }}
            - name: SPRING_DATA_MONGODB_USERNAME
              valueFrom:
                secretKeyRef:
                  name: {{ default (include "matcher-service.peerFullname" (dict "root" . "component" .Values.dependencies.dbComponentName)) .Values.databaseSecret.name }}
                  key: {{ .Values.databaseSecret.userKey }}
            - name: SPRING_DATA_MONGODB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: {{ default (include "matcher-service.peerFullname" (dict "root" . "component" .Values.dependencies.dbComponentName)) .Values.databaseSecret.name }}
                  key: {{ .Values.databaseSecret.passwordKey }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
