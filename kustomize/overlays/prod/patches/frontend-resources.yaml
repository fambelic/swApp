apiVersion: apps/v1
kind: Deployment
metadata:
  name: swapp-swapp-frontend
spec:
  template:
    spec:
      containers:
        - name: swapp-frontend
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
