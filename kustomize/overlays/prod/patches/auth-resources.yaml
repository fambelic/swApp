apiVersion: apps/v1
kind: Deployment
metadata:
  name: swapp-swapp-auth
spec:
  template:
    spec:
      containers:
        - name: swapp-auth
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
