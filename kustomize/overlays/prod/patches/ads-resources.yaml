apiVersion: apps/v1
kind: Deployment
metadata:
  name: swapp-swapp-ads
spec:
  template:
    spec:
      containers:
        - name: swapp-ads
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
