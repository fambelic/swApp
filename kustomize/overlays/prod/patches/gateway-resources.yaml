apiVersion: apps/v1
kind: Deployment
metadata:
  name: swapp-swapp-gateway
spec:
  template:
    spec:
      containers:
        - name: swapp-gateway
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
