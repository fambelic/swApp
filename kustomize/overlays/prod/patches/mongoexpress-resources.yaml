apiVersion: apps/v1
kind: Deployment
metadata:
  name: swapp-swapp-mongoexpress
spec:
  template:
    spec:
      containers:
        - name: swapp-mongoexpress
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
