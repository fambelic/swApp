apiVersion: apps/v1
kind: Deployment
metadata:
  name: swapp-swapp-auth
spec:
  template:
    spec:
      containers:
        - name: swapp-auth
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
