apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: swapp-swapp-db
spec:
  template:
    spec:
      containers:
        - name: swapp-db
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
