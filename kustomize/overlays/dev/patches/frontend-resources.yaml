apiVersion: apps/v1
kind: Deployment
metadata:
  name: swapp-swapp-frontend
spec:
  template:
    spec:
      containers:
        - name: swapp-frontend
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
