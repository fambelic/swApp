apiVersion: apps/v1
kind: Deployment
metadata:
  name: swapp-swapp-queue
spec:
  template:
    spec:
      containers:
        - name: swapp-queue
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "300m"
              memory: "256Mi"
