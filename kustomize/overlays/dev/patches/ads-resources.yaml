apiVersion: apps/v1
kind: Deployment
metadata:
  name: swapp-swapp-ads
spec:
  template:
    spec:
      containers:
        - name: swapp-ads
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
